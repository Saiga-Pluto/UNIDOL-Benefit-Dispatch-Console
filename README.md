# UNIDOL Benefit Dispatch Console

CSV / TSV / Excelをアップロードして、メール本文のプレビュー、送信対象の選択、GAS API経由のメール送信を行うWebアプリです。

## ディレクトリ

| パス | 内容 |
| --- | --- |
| `public/` | GitHub Pagesなどで公開する静的Webアプリ |
| `gas-send-api/` | Webアプリから呼び出す送信用GAS API |
| `spreadsheet-gas/` | 既存のスプレッドシート操作用GASの保存版 |

## 入力ファイル

1行目は見出し、2行目以降が送信対象です。

| 列 | 内容 |
| --- | --- |
| A | メールアドレス |
| B | 宛名 |
| C1 | 1つ目の特典見出し |
| C2以降 | 1つ目の特典のサブ名、担当者名など |
| D2以降 | 1つ目の特典リンク |
| E1 | 2つ目の特典見出し |
| E2以降 | 2つ目の特典のサブ名、担当者名など |
| F2以降 | 2つ目の特典リンク |
| G2以降 | `TRUE` なら初期状態で送信対象 |

1つ目の特典は、D列のリンクが入っている場合だけ本文に入ります。
2つ目の特典は、F列のリンクが入っている場合だけ本文に入ります。
C列とE列のサブ名は任意です。空の場合でも、リンクがあれば見出しとリンクだけ本文に入ります。

状態欄では、`1つ目リンクなし`、`1つ目サブ名なし`、`2つ目リンクなし`、`2つ目サブ名なし` を同時に確認できます。

要確認として件数に数え、行を薄い赤色で表示するのは、`メールなし` と `宛名なし` の2つだけです。

## ローカル確認

`public/index.html` をブラウザで開けば画面確認できます。

ローカルサーバーで確認する場合:

```sh
python3 -m http.server 5173 --directory public
```

## GitHub Pages

GitHub Pagesで公開する場合は、リポジトリの Pages 設定で公開元を対象ブランチの `/public` にしてください。

`main` ブランチのGAS API方式では、`gas-send-api/` のGASを別途デプロイし、Webアプリ画面の「送信用GAS API URL」に貼り付けます。

## Gmail APIログイン送信の実験ブランチ

`experiment-gmail-api-login` ブランチでは、送信用GAS API URLを使わず、Googleログインで取得したアクセストークンを使ってGmail APIから送信します。

この方式では、送信者はログインしたGoogleアカウントのGmailアドレスになります。Webアプリに必要なのは、Google Cloudで作成したOAuthクライアントIDです。

ユーザーにOAuthクライアントIDを入力させないため、クライアントIDはアプリ側に固定します。

`public/app.js` の以下を、Google Cloudで作成したOAuthクライアントIDに差し替えてください。

```js
const GOOGLE_OAUTH_CLIENT_ID = 'REPLACE_WITH_YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com';
```

### 必要なGoogle Cloud設定

1. Google Cloud Consoleでプロジェクトを作成する。
2. Gmail APIを有効化する。
3. OAuth同意画面を設定する。
4. OAuthクライアントIDを作成する。
5. アプリケーションの種類は `ウェブ アプリケーション` を選ぶ。
6. `承認済みの JavaScript 生成元` に公開URLを追加する。
   ローカル確認では `http://127.0.0.1:5173` などを追加する。
7. 作成された `クライアントID` を `public/app.js` の `GOOGLE_OAUTH_CLIENT_ID` に設定する。

### 注意点

この方式では `https://www.googleapis.com/auth/gmail.send` の権限を使います。運用対象が広い場合、GoogleのOAuth同意画面やアプリ確認が必要になる可能性があります。

既存のGAS API URL方式は `main` ブランチに残しています。

### OAuth申請用の説明文

GoogleのOAuth確認申請では、スコープごとに利用目的を説明します。

`https://www.googleapis.com/auth/gmail.send`

```text
このアプリは、ユーザーがアップロードしたCSV/Excelの送信リストに基づき、ユーザー自身が画面上で選択・プレビュー確認した宛先に対して、ユーザーのGmailアカウントからデジタル特典メールを送信するために Gmail send 権限を使用します。

このアプリはメールの読み取り、受信トレイの閲覧、既存メールの検索、メール本文の保存を行いません。送信処理はユーザーが明示的に「チェック済みを送信」を押した場合にのみ実行されます。
```

`https://www.googleapis.com/auth/userinfo.email`

```text
ログイン中のGoogleアカウントのメールアドレスを画面上に表示し、どのGmailアドレスから送信されるかをユーザーが確認できるようにするために使用します。
```

### プライバシーポリシー雛形

以下を公開ページとして用意し、OAuth同意画面の `Privacy Policy URL` に設定してください。

```text
# プライバシーポリシー

UNIDOL Benefit Dispatch Console（以下「本アプリ」）は、ユーザーがアップロードした送信リストに基づいて、ユーザー自身のGoogleアカウントからデジタル特典メールを送信するためのツールです。

## 取得する情報

本アプリは、Google認証を通じて以下の権限を要求します。

- Gmailからメールを送信する権限
- ログイン中のGoogleアカウントのメールアドレスを確認する権限

## 利用目的

取得した権限は、ユーザーが画面上で選択し、プレビュー確認した宛先へメールを送信するためにのみ使用します。ログイン中のメールアドレスは、送信元アカウントをユーザーに表示するために使用します。

## 保存する情報

本アプリは、アップロードされたCSV/Excelファイル、宛先メールアドレス、メール本文、Googleアクセストークンをサーバーに保存しません。処理はユーザーのブラウザ上で行われます。

## 第三者提供

本アプリは、取得した情報を第三者に販売、共有、提供しません。

## Googleユーザーデータの取り扱い

本アプリによるGoogleユーザーデータの利用は、ユーザーに明示された機能の提供に限定されます。Googleユーザーデータを広告、分析、信用評価、販売目的で使用しません。

## 問い合わせ先

問い合わせ先メールアドレス: [運営者のメールアドレス]
```

### 利用規約雛形

OAuth同意画面で `Terms of Service URL` を求められる場合は、以下を公開ページとして用意してください。

```text
# 利用規約

本アプリは、ユーザーが管理する送信リストに基づき、デジタル特典メールの作成、確認、送信を補助するツールです。

ユーザーは、送信先の同意または正当な送信理由がある場合に限り、本アプリを使用できます。スパム、迷惑メール、虚偽内容の送信、第三者の権利を侵害する利用は禁止します。

本アプリの利用により発生した送信内容、送信先、送信結果に関する責任はユーザーが負うものとします。

運営者は、本アプリの機能改善、保守、または不正利用防止のため、予告なく仕様を変更または提供を停止することがあります。
```

### 審査用デモ動画の手順

GoogleのOAuth確認では、アプリの実際の動作とスコープの使い道を示すデモ動画を求められることがあります。以下の流れを録画してください。

1. アプリのトップ画面を開く。
2. `Googleで接続` を押す。
3. Googleの同意画面を表示し、`gmail.send` の許可を求めていることを見せる。
4. 同意後、ログイン中のGoogleメールアドレスが画面に表示されることを見せる。
5. サンプルCSVまたは検証用CSVをアップロードする。
6. 宛先、特典リンク、状態欄、送信対象チェックを見せる。
7. 右側のプレビューで、送信前に本文を確認できることを見せる。
8. 送信対象を1件だけチェックし、`チェック済みを送信` を押す。
9. Gmail APIで送信が完了し、状態が送信済みになることを見せる。
10. 受信側メールボックスでメールが届いていることを見せる。
11. アプリがメール閲覧や受信トレイ表示を行わず、送信機能だけを提供していることを説明する。

動画内で説明するポイント:

- 本アプリはメール送信のみを行う。
- アップロードデータとメール本文はサーバーに保存しない。
- ユーザーが明示的に選択・確認した宛先にだけ送信する。
- 1回100件までの制限がある。

## 送信用GAS API URL

送信用GAS API URLは、`gas-send-api/` のApps Scriptをウェブアプリとしてデプロイしたときに発行される `/exec` で終わるURLです。

入手手順:

1. Google Apps Scriptで新しいプロジェクトを作成する。
2. `gas-send-api/Code.js` と `gas-send-api/appsscript.json` を配置する。
3. 右上の `デプロイ` から `新しいデプロイ` を選ぶ。
4. 種類で `ウェブアプリ` を選ぶ。
5. 実行ユーザーを `自分` にする。
6. アクセスできるユーザーを運用に合わせて選ぶ。
7. デプロイ後に表示される `ウェブアプリ URL` をコピーする。
8. Webアプリ画面の `送信用GAS API URL` に貼り付ける。

このURLを通じて、Web画面で作った送信内容をGASに渡し、GAS側が `GmailApp.sendEmail` でメールを送信します。
